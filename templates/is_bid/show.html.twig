{% extends 'base.html.twig' %}

{% block title %}IsBid{% endblock %}

{% block body %}
    <h1>IsBid</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ is_bid.id }}</td>
            </tr>
            <tr>
                <th>IsBid</th>
                <td>{{ is_bid.isBid ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('is_bid_index') }}">back to list</a>

    <a href="{{ path('is_bid_edit', {'id': is_bid.id}) }}">edit</a>

    {{ include('is_bid/_delete_form.html.twig') }}
{% endblock %}
